const importHtmlEntry = (url) => {
  const template = document.createElement('div')
  /**
   * TODO: 获取所有的script 标签代码
   */
  function getExternalScripts () {

  }
  /**
   * TODO: 获取并执行执行所有script 标签代码
   */
  function execScripts () {

  }
  return {
    template,
    getExternalScripts,
    execScripts
  }
}
export default importHtmlEntry
